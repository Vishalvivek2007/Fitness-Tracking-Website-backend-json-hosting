{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0 --no-static"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": "18.x"
  }
}
